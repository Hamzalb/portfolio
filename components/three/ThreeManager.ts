// ThreeManager is disabled — WebGL not available on this device.
// Each 3D component now uses Canvas 2D or CSS 3D instead.
export interface SceneEntry { id: string }
export const ThreeManager = {
  register: async () => null,
  unregister: () => {},
};
